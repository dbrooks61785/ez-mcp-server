{
  "name": "EZ Automation MCP",
  "version": "1.0.0",
  "tools": {
    "ping": {
      "description": "Test the MCP server",
      "endpoint": "/mcp/execute",
      "method": "POST",
      "schema": {
        "type": "object",
        "properties": {
          "tool": { "type": "string" }
        },
        "required": ["tool"]
      }
    }
  }
}
